# 核心依赖 - 版本兼容性修复
torch>=2.0.0
transformers<4.58
ms-swift<3.10
modelscope>=1.30.0
datasets>=2.10.0,<2.17.0  # 放宽版本约束以兼容modelscope的LargeList需求
pyarrow>=8.0.0,<15.0.0  # pyarrow版本约束，确保与datasets兼容

# 数据处理和分析
pandas
numpy<2.0.0  # NumPy 2.x与很多包不兼容，强制使用1.x
scikit-learn
matplotlib
seaborn
jieba
tqdm

# 可视化和词云
wordcloud
plotly

# 网络请求
requests

# 开发依赖（可选）
# jupyter
# ipython
